# No Rules

`targets/05_logerror` is intentionally included without rewrite rules.

This target is a negative experiment case: the transformation is too hard to
describe with the current Pcode Weaver DSL, so it documents a limitation rather
than a supported rewrite.
